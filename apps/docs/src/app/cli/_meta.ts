import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  scanning: "Scanning",
  authentication: "Authentication",
  projects: "Projects",
  pull: "Pull & Push",
};

export default meta;
