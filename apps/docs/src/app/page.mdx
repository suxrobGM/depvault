# DepVault Documentation

DepVault helps developers manage dependencies and secrets in one place. Scan for vulnerabilities, store your repo's config and secret files with end-to-end encryption, and share secrets securely — across any tech stack.

## What is DepVault?

DepVault provides three core tools in one unified platform:

- **Dependency Analysis** — Scan dependency files from 9+ ecosystems (Node.js, Python, .NET, Rust, Go, Java, Kotlin, Ruby, PHP) and get instant reports on outdated packages, known CVEs, and license conflicts.
- **Encrypted Config & Secret Files** — Store your repository's config files (`.env`, `appsettings.json`, …) and secret files (certificates, keys, credentials) with zero-knowledge encryption. Your vault password never leaves your device — each file is encrypted with AES-256-GCM in your browser before it reaches the server.
- **Secret Sharing & CI/CD** — Share secrets via one-time encrypted links, restore files into CI/CD pipelines with scoped tokens, and scan git repos for accidentally committed credentials.

## Why DepVault?

Most secret managers require you to trust the server with your data. DepVault is different — secrets are encrypted client-side before they ever leave your browser.
The server stores only ciphertext and can never decrypt your data. The full codebase is open source so you can audit every line.

## Quick Links

- [**Getting Started**](/getting-started) — Create your account, set up your first project, and run your first scan.
- [**Guides**](/guides) — Feature walkthroughs for the web dashboard and CLI.
- [**CLI Reference**](/cli) — Install and configure the DepVault CLI.

## Platform Overview

| Interface         | Description                                                        |
| ----------------- | ------------------------------------------------------------------ |
| **Web Dashboard** | Full-featured web app for managing projects, secrets, and analysis |
| **CLI**           | Native AOT binary for terminal access and CI/CD pipelines          |

## Supported Ecosystems

| Ecosystem | Files                                           |
| --------- | ----------------------------------------------- |
| Node.js   | `package.json`,                                 |
| Python    | `requirements.txt`, `pyproject.toml`, `Pipfile` |
| .NET      | `*.csproj`, `Directory.Packages.props`          |
| Rust      | `Cargo.toml`                                    |
| Go        | `go.mod`                                        |
| Java      | `pom.xml`                                       |
| Kotlin    | `build.gradle.kts`, `libs.versions.toml`        |
| Ruby      | `Gemfile`                                       |
| PHP       | `composer.json`                                 |

## Supported Config Formats

DepVault stores any plaintext config file as a client-encrypted blob, including:

- `.env` (dotenv)
- `appsettings.json` (ASP.NET)
- `secrets.yaml` (Kubernetes)
- `config.toml` (TOML)
